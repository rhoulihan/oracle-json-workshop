{
  "id": "module-5",
  "title": "Multi-Protocol Access",
  "description": "Access the same data through MongoDB wire protocol and JavaScript. Prove protocol convergence \u2014 a MongoDB app can connect to Oracle without changing a single line of driver code.",
  "estimatedTime": 7,
  "checkpoint": "You just wrote to relational tables using the MongoDB wire protocol. A MongoDB application can connect to Oracle without changing a single line of driver code.",
  "exercises": [
    {
      "id": "5.1",
      "title": "MongoDB API via mongosh",
      "description": "List collections, query documents, insert through MongoDB API, and verify via SQL.",
      "explanation": "Oracle's **ORDS (Oracle REST Data Services)** implements the MongoDB wire protocol natively, meaning **mongosh** \u2014 the official MongoDB shell \u2014 connects to Oracle and issues `find()`, `insertOne()`, `deleteOne()` against the same tables and collections you have been querying with SQL. This is not an emulation layer or a compatibility shim; the wire protocol translates MongoDB operations into Oracle SQL internally, so every operation benefits from the cost-based optimizer, B-tree indexes, and ACID transactions. Duality views appear as MongoDB collections, so an `insertOne()` through mongosh actually writes to **normalized relational tables** via the duality view's bidirectional mapping \u2014 the document is decomposed into rows across multiple tables in a single transaction. No ETL, no sync jobs, no driver changes, no application rewrites. The `show collections` command reveals both native JSON collections and duality views as MongoDB-compatible collections, giving your existing MongoDB applications a migration path that does not require rewriting a single line of driver code. The key parameters to know: your connection string uses the ORDS endpoint URL with the MongoDB API path, and authentication maps to Oracle database credentials. **Try this:** Run `db.CLIENT_INTERACTIONS.find({\"sentiment\": \"positive\"}).limit(5)` to filter by sentiment, or try `db.CLIENT_INTERACTIONS.countDocuments({\"type\": \"review\"})` to see MongoDB aggregation working against Oracle storage. You can also run `db.ADVISORY_ENTITIES.find({\"entityType\": \"holding\", \"data.tags\": \"dividend\"})` to query the single-table collection from Module 3 using MongoDB syntax.",
      "codeType": "mongo",
      "code": "// List collections (includes both JSON collections and Duality Views)\nshow collections\n\n// Query a collection\ndb.CLIENT_INTERACTIONS.find({\"client_id\": 3}).limit(3)\n\n// Query a Duality View as if it were a MongoDB collection\ndb.CLIENT_PORTFOLIO_DV.find({\"_id\": 3})\n\n// Insert through MongoDB API \u2014 writes to relational tables via Duality View\ndb.CLIENT_PORTFOLIO_DV.insertOne({\n    \"firstName\": \"Workshop\",\n    \"lastName\": \"Developer\",\n    \"email\": \"dev@workshop.local\",\n    \"riskProfile\": \"moderate\",\n    \"status\": \"active\",\n    \"accounts\": []\n})\n\n// Clean up\ndb.CLIENT_PORTFOLIO_DV.deleteOne({\"email\": \"dev@workshop.local\"})",
      "steps": [
        {
          "label": "List all collections visible through MongoDB wire protocol",
          "code": "show collections"
        },
        {
          "label": "Query the client_interactions collection",
          "code": "db.CLIENT_INTERACTIONS.find({\"client_id\": 3}).limit(3)"
        },
        {
          "label": "Query a duality view as a MongoDB collection",
          "code": "db.CLIENT_PORTFOLIO_DV.find({\"_id\": 3})"
        },
        {
          "label": "Insert through MongoDB API \u2014 writes to relational tables via duality view",
          "code": "db.CLIENT_PORTFOLIO_DV.insertOne({\n    \"firstName\": \"Workshop\",\n    \"lastName\": \"Developer\",\n    \"email\": \"dev@workshop.local\",\n    \"riskProfile\": \"moderate\",\n    \"status\": \"active\",\n    \"accounts\": []\n})"
        },
        {
          "label": "Clean up the test record",
          "code": "db.CLIENT_PORTFOLIO_DV.deleteOne({\"email\": \"dev@workshop.local\"})"
        }
      ],
      "validationQuery": "SELECT COUNT(*) AS cnt FROM client_interactions ci WHERE ci.data.client_id.number() = 3",
      "expectedResultPattern": "rowCount:>0"
    },
    {
      "id": "5.2",
      "title": "JavaScript with node-oracledb",
      "description": "Query duality views, insert with JSON type binding, and use the SODA API \u2014 all from JavaScript.",
      "explanation": "The **node-oracledb** driver gives JavaScript applications three distinct access patterns against the same Oracle database, and all three hit the same data, the same indexes, the same optimizer. First, **SQL queries against duality views** return native JavaScript objects \u2014 the JSON column maps directly to JS objects without any `JSON.parse()` call because Oracle's OSON binary format is deserialized natively by the driver. Second, **JSON type binding** (`oracledb.DB_TYPE_JSON`) lets you insert JavaScript objects directly into JSON collections without manual serialization \u2014 Oracle converts them to binary OSON on ingest, which means O(1) field access on every subsequent read. Third, the **SODA (Simple Oracle Document Access)** API provides a document-store interface with `createCollection()`, `insertOne()`, and `find().filter()` \u2014 the same mental model as MongoDB's driver API but backed by Oracle's transactional engine with full ACID guarantees. The key parameter to understand is `outFormat: oracledb.OUT_FORMAT_OBJECT`, which tells the driver to return rows as JavaScript objects rather than arrays \u2014 critical for readable code. For inserts, the `{ val: myObject, type: oracledb.DB_TYPE_JSON }` binding syntax tells Oracle to store the value as native OSON rather than serialized text, which is a significant performance advantage for large documents. **Try this:** After the duality view query, explore the nested structure with `console.log(JSON.stringify(clientDoc.accounts[0], null, 2))` to see account details, or try querying with a different client ID like `{ id: 1002 }` to compare portfolio structures across clients.",
      "codeType": "js",
      "code": "// 1. Query a Duality View \u2014 returns native JavaScript objects\nconst result = await connection.execute(\n    `SELECT data FROM client_portfolio_dv WHERE json_value(data, '$._id' RETURNING NUMBER) = :id`,\n    { id: 3 },\n    { outFormat: oracledb.OUT_FORMAT_OBJECT }\n);\nconst clientDoc = result.rows[0].DATA;\nconsole.log('Client:', clientDoc.firstName, clientDoc.lastName);\nconsole.log('Accounts:', clientDoc.accounts.length);\n\n// 2. Insert via JSON type binding\nconst newInteraction = {\n    client_id: 3,\n    advisor_id: 5,\n    type: \"workshop_test\",\n    date: new Date().toISOString(),\n    channel: \"api\",\n    summary: \"Inserted from JavaScript via node-oracledb\",\n    action_items: [],\n    sentiment: \"excited\",\n    follow_up_required: false\n};\n\nawait connection.execute(\n    `INSERT INTO client_interactions (data) VALUES (:doc)`,\n    { doc: { val: newInteraction, type: oracledb.DB_TYPE_JSON } }\n);\nawait connection.commit();\nconsole.log('Inserted interaction successfully');\n\n// 3. Verify and clean up\nawait connection.execute(\n    `DELETE FROM client_interactions WHERE data.type.string() = 'workshop_test'`\n);\nawait connection.commit();\nconsole.log('Cleanup complete');",
      "steps": [
        {
          "label": "Query a duality view \u2014 returns native JavaScript objects",
          "code": "const result = await connection.execute(\n    `SELECT data FROM client_portfolio_dv WHERE json_value(data, '$._id' RETURNING NUMBER) = :id`,\n    { id: 3 },\n    { outFormat: oracledb.OUT_FORMAT_OBJECT }\n);\nconst clientDoc = result.rows[0].DATA;\nconsole.log('Client:', clientDoc.firstName, clientDoc.lastName);\nconsole.log('Accounts:', clientDoc.accounts.length);"
        },
        {
          "label": "Insert a document via JSON type binding",
          "code": "const newInteraction = {\n    client_id: 3,\n    advisor_id: 5,\n    type: \"workshop_test\",\n    date: new Date().toISOString(),\n    channel: \"api\",\n    summary: \"Inserted from JavaScript via node-oracledb\",\n    action_items: [],\n    sentiment: \"excited\",\n    follow_up_required: false\n};\n\nawait connection.execute(\n    `INSERT INTO client_interactions (data) VALUES (:doc)`,\n    { doc: { val: newInteraction, type: oracledb.DB_TYPE_JSON } }\n);\nawait connection.commit();\nconsole.log('Inserted interaction successfully');"
        },
        {
          "label": "Verify and clean up the test record",
          "code": "await connection.execute(\n    `DELETE FROM client_interactions WHERE data.type.string() = 'workshop_test'`\n);\nawait connection.commit();\nconsole.log('Cleanup complete');"
        }
      ],
      "validationQuery": "SELECT COUNT(*) AS cnt FROM client_portfolio_dv WHERE json_value(data, '$._id' RETURNING NUMBER) = 3",
      "expectedResultPattern": "rowCount:1"
    }
  ]
}
